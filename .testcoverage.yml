profile: cover.out

threshold:
  # Minimum overall project coverage percentage required
  total: 60

# Exclude auxiliary specification package from coverage stats
exclude:
  paths:
    - ^shared/spec(/|$)
